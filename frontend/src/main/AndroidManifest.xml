<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CineMobile">

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CineMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.EventsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.EventDetailActivity"
            android:exported="false" />

        <!-- Puedes conservar MainActivity si lo deseas -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- dentro de <application> -->
        <activity android:name=".ui.EventsActivity" android:exported="false" />
        <activity android:name=".ui.EventDetailActivity" android:exported="false" />

    </application>

</manifest>